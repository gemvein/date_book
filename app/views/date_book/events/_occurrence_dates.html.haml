%dl.dl-horizontal
  %dt= :previous_occurrence.l
  %dd= render_date_range event.previous_occurrence.start_date, event.previous_occurrence.end_date, event.schedule.all_day, event.schedule.duration
  %dt= :next_occurrence.l
  %dd= render_date_range event.next_occurrence.start_date, event.next_occurrence.end_date, event.schedule.all_day, event.schedule.duration

