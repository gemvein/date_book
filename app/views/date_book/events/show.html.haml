- add_title @event.name
= date_book_scripts
= event_occurrence_dates @event
= @event.description.html_safe

- if can? :edit, @event
  = icon_link_to 'pencil', :edit.l, edit_event_path(@event), class: 'btn btn-primary'
